spring:
  profiles:
    active: ${SPRING_PROFILES_ACTIVE:dev}

  application:
    name: program-service

  config:
    import: "configserver:"

  cloud:
    config:
      uri: ${CONFIG_SERVER_URI:http://localhost:9000}
